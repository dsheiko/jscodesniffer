
[
    {
        "describe": "Inner grouping parens shall have no spaces around",
        "expected": "Idiomatic.invalidOutterGroupingLeadingSpacing",
        "js": "if (!('foo' in obj)) {}"
    },
    {
        "describe": "Inner grouping parens shall have no spaces around",
        "expected": "",
        "js": "if ( !('foo' in obj) ) {}"
    }
]